    <!-- Navbar  -->
    <nav class="navbar fixed-top navbar-expand-lg navbar-dark p-md-3" id="navbar">
      <div class="container">
        <a class="navbar-brand" href="#">VeSkaters</a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
          aria-controls="navbarNav"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div id="box-nav" class="box-nav">
          <form action="#">
        <input type="text" name="" placeholder="   Type...">
        <input type="submit" name="" value="Search">
      </form>
        </div>
        <div class="collapse navbar-collapse" id="navbarNav">
          <div class="mx-auto"></div>
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link text-white" href="/" id="home">Inicio</a>
            </li>
            <li class="nav-item">
              <a class="nav-link text-white" href="/skates" id="home">Producos</a>
            </li>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <% if user_signed_in? %>
                <i class="fas fa-sign-in-alt"></i>
                <%else%>
                  <i class="fas fa-user-plus"></i>
                <%end%>
              </a>
              <div class="dropdown-menu rounded" aria-labelledby="navbarDropdown">
                <% if user_signed_in? %>
                <a class="dropdown-item" href="#"><%= current_user.email%></a>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="/skates/new">Add product</a>
                <a class="dropdown-item" href="#">Desconectar</a>
                <%= link_to "Salir", destroy_user_session_path(current_user), method: :delete, data: { confirm: "Seguro de cerrar sesion?" }, class: "dropdown-item" %>
                <%else%>
                <a class="dropdown-item" href="/users/sign_up">regístrate</a>
                <a class="dropdown-item" href="/users/sign_in">Inicia sesión</a>
                <%end%>
              </div>
            </li>
          </ul>
        </div>
      </div>
    </nav>
